import 'package:flutter/material.dart';

class SignupMenuButton extends StatelessWidget {
  const SignupMenuButton({super.key});

  @override
  Widget build(BuildContext context) {
    return ElevatedButton(onPressed: () {}, child: Text('teste'));
  }
}
